import { json,parseBody,requireProfile,errResponse,presignR2 } from './_lib.mjs';
export const handler=async event=>{if(event.httpMethod!=='POST')return json(405,{error:'Método não permitido.'});try{await requireProfile(event,{admin:true});const {key}=parseBody(event);if(!key||!['assets/','previews/','covers/'].some(p=>String(key).startsWith(p)))return json(400,{error:'Chave inválida.'});const url=presignR2({method:'DELETE',key,expires:60});const r=await fetch(url,{method:'DELETE'});if(!r.ok&&r.status!==404)throw Object.assign(new Error('Falha ao remover objeto do R2.'),{status:502});return json(200,{ok:true});}catch(e){return errResponse(e)}};
