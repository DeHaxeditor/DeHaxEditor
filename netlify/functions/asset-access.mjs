import { json,parseBody,requireProfile,sb,getSetting,activePro,clientHash,errResponse,safeFilename,presignR2 } from './_lib.mjs';
export const handler=async event=>{if(event.httpMethod!=='POST')return json(405,{error:'Método não permitido.'});try{const {user,p}=await requireProfile(event);const {assetId,action='download'}=parseBody(event);if(!assetId||!['download','preview'].includes(action))return json(400,{error:'Solicitação inválida.'});const globalEnabled=await getSetting('global_downloads_enabled',true);if(action==='download'&&globalEnabled===false)throw Object.assign(new Error('Downloads estão temporariamente pausados pela administração.'),{status:503});const {data:rows}=await sb(`/rest/v1/assets?id=eq.${encodeURIComponent(assetId)}&select=*`);const a=rows?.[0];if(!a||a.status!=='published')throw Object.assign(new Error('Este asset não está disponível.'),{status:404});if(a.access_level==='pro'&&!activePro(p))throw Object.assign(new Error('Este conteúdo é exclusivo para membros PRO.'),{status:403});if(action==='download'&&!a.download_enabled)throw Object.assign(new Error('O download deste item está temporariamente pausado.'),{status:423});if(action==='preview'&&!a.preview_enabled)throw Object.assign(new Error('O preview deste item está temporariamente pausado.'),{status:423});const {data:privateRows}=await sb(`/rest/v1/asset_files?asset_id=eq.${encodeURIComponent(assetId)}&select=file_key,preview_key`);const f=privateRows?.[0];const key=action==='preview'?(f?.preview_key||((a.media_type==='audio'||a.media_type==='video'||a.media_type==='image')?f?.file_key:null)):f?.file_key;if(!key)throw Object.assign(new Error(action==='preview'?'Este item ainda não possui preview.':'Arquivo ainda não foi enviado.'),{status:404});if(action==='download'){const settingLimit=Number(await getSetting('download_daily_limit',Number(process.env.DOWNLOAD_DAILY_LIMIT||500)))||500;const since=new Date(Date.now()-864e5).toISOString();const {data:dls}=await sb(`/rest/v1/downloads?user_id=eq.${encodeURIComponent(user.id)}&created_at=gte.${encodeURIComponent(since)}&action=eq.download&select=id&limit=${settingLimit+1}`);if((dls?.length||0)>=settingLimit)throw Object.assign(new Error(`Limite de segurança atingido (${settingLimit} downloads em 24h). Entre em contato com o suporte se estiver em uso legítimo.`),{status:429})}
const ext=(String(key).match(/\.([A-Za-z0-9]{1,8})$/)||[])[1]||String(a.file_format||'').toLowerCase().replace(/[^a-z0-9]/g,'').slice(0,8);const filename=safeFilename(`${a.title||'dehax'}${ext?'.'+ext:''}`);const url=presignR2({method:'GET',key,expires:120,responseDisposition:action==='download'?`attachment; filename="${filename}"`:'inline'});if(action==='download'){await sb('/rest/v1/downloads',{method:'POST',headers:{Prefer:'return=minimal'},body:{user_id:user.id,asset_id:a.id,action:'download',ip_hash:clientHash(event)}});try{await sb('/rest/v1/rpc/increment_asset_download',{method:'POST',body:{asset_uuid:a.id}})}catch{}}
return json(200,{url,expiresIn:120,action});}catch(e){return errResponse(e)}};
