import {json,parseBody,requireProfile,env,errResponse} from './_lib.mjs';
export const handler=async event=>{if(event.httpMethod!=='POST')return json(405,{error:'Método não permitido.'});try{await requireProfile(event);const body=parseBody(event),base=env('VOD_SERVICE_URL').replace(/\/$/,'');const r=await fetch(base+'/analyze',{method:'POST',headers:{'Content-Type':'application/json','X-Internal-Token':env('VOD_INTERNAL_TOKEN')},body:JSON.stringify({url:body.url,playlist:!!body.playlist})});const data=await r.json().catch(()=>({}));if(!r.ok)throw Object.assign(new Error(data.detail||data.error||'Falha ao analisar o vídeo.'),{status:r.status});return json(200,data)}catch(e){return errResponse(e)}};
